pub struct Matrix{
    mat: Vec<Vec<Mint>>,
}

impl Matrix{
    fn new() -> Self{
        
    }
    fn mul(&self, &rhs:Matrix) -> Self{
        
    }
    
    fn pow(&self, n:usize) -> Self{
        
    }
}
